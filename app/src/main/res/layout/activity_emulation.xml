<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="84dp"
        android:orientation="vertical"
        android:paddingHorizontal="8dp">
        <TextView android:id="@+id/sessionStatus" android:layout_width="match_parent" android:layout_height="34dp" android:gravity="center_vertical" android:text="Starting core…" android:textColor="@color/accent" android:textSize="12sp" android:textStyle="bold" />
        <LinearLayout android:layout_width="match_parent" android:layout_height="46dp" android:gravity="center" android:orientation="horizontal">
            <Button android:id="@+id/pauseButton" style="@style/EmuButton.Toolbar" android:text="Pause" />
            <Button android:id="@+id/fastButton" style="@style/EmuButton.Toolbar" android:text="FF" />
            <Button android:id="@+id/quickSaveButton" style="@style/EmuButton.Toolbar" android:contentDescription="Choose a save-state slot" android:text="Save..." />
            <Button android:id="@+id/quickLoadButton" style="@style/EmuButton.Toolbar" android:contentDescription="Choose a load-state slot" android:text="Load..." />
            <Button android:id="@+id/resetButton" style="@style/EmuButton.Toolbar" android:text="Reset" />
            <Button android:id="@+id/menuButton" style="@style/EmuButton.Toolbar" android:text="Menu" />
        </LinearLayout>
    </LinearLayout>

    <FrameLayout
        android:id="@+id/sessionBody"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <FrameLayout
            android:id="@+id/surfaceHost"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="#000000">

            <com.beefbeefs.emuall.GameSurfaceView
                android:id="@+id/gameSurface"
                android:layout_width="match_parent"
                android:layout_height="match_parent" />
        </FrameLayout>

        <FrameLayout
            android:id="@+id/gameControls"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="12dp">
        <GridLayout android:id="@+id/directionalPad" android:layout_width="156dp" android:layout_height="156dp" android:layout_gravity="start|center_vertical" android:columnCount="3" android:rowCount="3">
            <Space android:layout_width="52dp" android:layout_height="52dp" />
            <Button android:id="@+id/upButton" style="@style/EmuButton.Pad" android:text="▲" />
            <Space android:layout_width="52dp" android:layout_height="52dp" />
            <Button android:id="@+id/leftButton" style="@style/EmuButton.Pad" android:text="◀" />
            <Space android:layout_width="52dp" android:layout_height="52dp" />
            <Button android:id="@+id/rightButton" style="@style/EmuButton.Pad" android:text="▶" />
            <Space android:layout_width="52dp" android:layout_height="52dp" />
            <Button android:id="@+id/downButton" style="@style/EmuButton.Pad" android:text="▼" />
        </GridLayout>
        <LinearLayout android:id="@+id/actionButtons" android:layout_width="170dp" android:layout_height="wrap_content" android:layout_gravity="end|center_vertical" android:gravity="center" android:orientation="vertical">
            <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content" android:gravity="center" android:orientation="horizontal">
                <Button android:id="@+id/lButton" style="@style/EmuButton.Shoulder" android:text="L" />
                <Button android:id="@+id/rButton" style="@style/EmuButton.Shoulder" android:text="R" />
            </LinearLayout>
            <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content" android:layout_marginTop="16dp" android:gravity="center" android:orientation="horizontal">
                <Button android:id="@+id/bButton" style="@style/EmuButton.Round" android:text="B" />
                <Button android:id="@+id/aButton" style="@style/EmuButton.Round" android:layout_marginStart="20dp" android:text="A" />
            </LinearLayout>
        </LinearLayout>
        <LinearLayout android:id="@+id/centerButtons" android:layout_width="wrap_content" android:layout_height="wrap_content" android:layout_gravity="center_horizontal|bottom" android:orientation="horizontal">
            <Button android:id="@+id/selectButton" style="@style/EmuButton.Center" android:text="Select" />
            <Button android:id="@+id/startButton" style="@style/EmuButton.Center" android:layout_marginStart="12dp" android:text="Start" />
        </LinearLayout>
        </FrameLayout>
    </FrameLayout>
</LinearLayout>
